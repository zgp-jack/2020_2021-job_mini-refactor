import * as React from 'react';
import AntPicker from '@ant-design/react-native/lib/picker';
import regionData from './regions.formatted';
import { noop } from '../../utils';
export default class RegionSelector extends React.Component {
    constructor() {
        super(...arguments);
        this.state = {
            value: []
        };
        this.onChange = (value) => {
            const { onChange = noop } = this.props;
            // 通过 value 查找 code
            let tmp = regionData;
            const code = value.map((item) => {
                for (let i = 0; i < tmp.length; i++) {
                    if (tmp[i].value === item) {
                        const code = tmp[i].code;
                        tmp = tmp[i].children || [];
                        return code;
                    }
                }
            });
            onChange({ detail: { value, code } });
        };
        this.onPickerChange = (value) => {
            this.setState({ value });
        };
        this.onDismiss = () => {
            const { onCancel = noop } = this.props;
            onCancel();
        };
    }
    static getDerivedStateFromProps(nextProps, lastState) {
        if (nextProps.value !== lastState.value) {
            return {
                value: nextProps.value
            };
        }
        return null;
    }
    render() {
        const { children, disabled, } = this.props;
        const { value, } = this.state;
        return (React.createElement(AntPicker, { data: regionData, value: value, onChange: this.onChange, onPickerChange: this.onPickerChange, onDismiss: this.onDismiss, disabled: disabled }, children));
    }
}
RegionSelector.defaultProps = {
    value: [],
};
