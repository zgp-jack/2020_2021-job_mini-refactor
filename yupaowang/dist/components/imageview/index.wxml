<block wx:if="{{$taroCompReady}}">
    <view class="uploads-imgs-lists clearfix">
        <block wx:if="{{images}}">
            <view class="uploads-img-item" wx:key="$loopState__temp2" bindtap="anonymousFunc0" wx:for="{{loopArray103}}" wx:for-item="item" wx:for-index="index" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey}}">
                <view class="uploads-img-imgbox">
                    <image class="uploads-item-img" src="{{item.$original.httpurl}}"></image>
                    <block wx:if="{{bool}}">
                        <view class="uploads-img-del" catchtap="anonymousFunc1" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey2}}"><text class="at-icon at-icon-trash"></text>
                        </view>
                    </block>
                </view>
            </view>
        </block>
        <block wx:if="{{max > images.length}}">
            <view class="uploads-img-item">
                <view class="uploads-img-imgbox uploads-img-imgbox-active" bindtap="anonymousFunc2"></view>
            </view>
        </block>
    </view>
</block>