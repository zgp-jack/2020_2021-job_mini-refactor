<block wx:if="{{$taroCompReady}}">
    <view class="profession-container">
        <view class="profession-content">
            <view class="profession-header">{{title}}</view>
            <scroll-view scroll-y="{{true}}" class="profession-body">
                <block wx:if="{{data}}">
                    <view class="profession-list-item" wx:key="id" wx:for="{{loopArray161}}" wx:for-item="item" wx:for-index="i">
                        <view class="profession-list-title">{{item.$original.name}}</view>
                        <view class="profession-list-content">
                            <view class="prosession-item overwords" bindtap="anonymousFunc0" wx:key="id" wx:for="{{item.$anonymousCallee__39}}" wx:for-item="d" wx:for-index="k" data-e-tap-so="this" data-e-tap-a-a="{{d._$indexKey}}">{{d.$original.name}}
                                <view class="{{d.$loopState__temp2}}"></view>
                            </view>
                        </view>
                    </view>
                </block>
            </scroll-view>
            <view class="profession-footer" bindtap="anonymousFunc1">{{footerTitle}}</view>
        </view>
    </view>
</block>