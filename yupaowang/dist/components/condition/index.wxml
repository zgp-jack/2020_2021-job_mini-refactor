<block wx:if="{{$taroCompReady}}">
    <block>
        <view class="recruit-condition-box">
            <block wx:if="{{data}}">
                <view class="recruit-condition-item" wx:key="id" bindtap="anonymousFunc0" wx:for="{{loopArray8}}" wx:for-item="item" wx:for-index="__index0" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey}}">
                    <view class="recruit-condition-content overwords">{{item.$original.text}}
                        <image class="recruit-condition-select" src="{{IMGCDNURL + 'select.png'}}"></image>
                    </view>
                </view>
            </block>
        </view>
        <at-drawer compid="{{$compid__9}}">
            <view class="common-drawer-item">
                <scroll-view class="drawer-full-lists" scroll-y="{{true}}">
                    <view class="{{item.$loopState__temp3}}" wx:key="id" bindtap="anonymousFunc2" wx:for="{{loopArray9}}" wx:for-item="item" wx:for-index="index" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey2}}">{{item.$original.name}}</view>
                </scroll-view>
                <block wx:if="{{childAreaList.length}}">
                    <scroll-view class="drawer-full-lists drawer-half-lists" scroll-y="{{true}}" scroll-top="{{scrollTop}}" bindscroll="anonymousFunc3">
                        <view class="drawer-list-item overwords" wx:for="{{childAreaList}}" wx:for-item="item" wx:for-index="_anonIdx2">{{item.name}}</view>
                    </scroll-view>
                </block>
            </view>
        </at-drawer>
    </block>
</block>