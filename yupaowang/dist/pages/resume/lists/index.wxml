<block wx:if="{{$taroCompReady}}">
    <view class="recruit-container">
        <view class="recruit-fiexd-header">
            <search compid="{{$compid__93}}"></search>
            <condition compid="{{$compid__94}}"></condition>
        </view>
        <scroll-view class="recruit-lists-containerbox" scroll-y="{{true}}" lower-threshold="{{200}}" refresher-enabled="{{true}}" refresher-triggered="{{refresh}}" bindrefresherrefresh="anonymousFunc0" bindscrolltolower="anonymousFunc1">
            <view style="{{anonymousState__temp}}"></view>
            <wechat-notice></wechat-notice>
            <resume-list compid="{{$compid__95}}"></resume-list>
        </scroll-view>
    </view>
</block>