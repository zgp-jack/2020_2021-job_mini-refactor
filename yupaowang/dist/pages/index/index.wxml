<block wx:if="{{$taroCompReady}}">
    <view>
        <block wx:if="{{tabKey === HOME}}">
            <home></home>
        </block>
        <block wx:if="{{tabKey === RECRUIT}}">
            <recruit compid="{{$compid__0}}"></recruit>
        </block>
        <block wx:if="{{tabKey === RESUME}}">
            <resume></resume>
        </block>
        <block wx:if="{{tabKey === MEMBER}}">
            <member></member>
        </block>
        <tabbar></tabbar>
    </view>
</block>