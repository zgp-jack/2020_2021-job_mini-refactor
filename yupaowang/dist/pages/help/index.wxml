<block wx:if="{{$taroCompReady}}">
    <view class="help-content">
        <block wx:if="{{!data.item.length}}">
            <nodata></nodata>
        </block>
        <scroll-view class="recruit-lists-containerbox" scroll-y="{{true}}" lower-threshold="{{200}}" refresher-enabled="{{true}}" refresher-triggered="{{refresh}}" bindrefresherrefresh="anonymousFunc0" bindscrolltolower="anonymousFunc1">
            <view class="topNoneBox"></view>
            <at-accordion wx:key="id" wx:for="{{loopArray74}}" wx:for-item="item" wx:for-index="__index2" compid="{{item.$compid__62}}">
                <at-list compid="{{item.$compid__63}}"><text class="help-text">{{item.$original.answer}}</text>
                </at-list>
            </at-accordion>
            <block wx:if="{{!isDown && data.item.length}}">
                <view class="help-noData">没有更多数据了</view>
            </block>
            <view class="BootomNoneBox"></view>
        </scroll-view>
        <view class="help-button-box" bindtap="anonymousFunc3"><text class="help-button">意见反馈</text>
        </view>
    </view>
</block>